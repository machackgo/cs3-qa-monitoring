name: CS3 CI/CD Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy-and-verify:
    runs-on: [self-hosted, linux, x64, cs3]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Runner info
        run: |
          echo "Runner is working"
          whoami
          hostname
          pwd

      - name: Show repository structure
        run: |
          ls -la
          ls -la /home/group04/cs3/cs3-qa-monitoring
          ls -la /home/group04/cs3/cs3-qa-monitoring/qa-local
          ls -la /home/group04/cs3/cs3-qa-monitoring/qa-api
          ls -la /home/group04/cs3/cs3-qa-monitoring/monitoring

      - name: Check Docker
        run: |
          docker --version
          docker compose version
          docker ps

      - name: Deploy qa-local
        run: |
          cd /home/group04/cs3/cs3-qa-monitoring/qa-local
          docker compose down || true
          docker compose up -d --build
          docker compose ps

      - name: Deploy qa-api
        run: |
          cd /home/group04/cs3/cs3-qa-monitoring/qa-api
          docker compose down || true
          docker compose up -d --build
          docker compose ps

      - name: Deploy monitoring
        run: |
          cd /home/group04/cs3/cs3-qa-monitoring/monitoring
          docker compose down || true
          docker compose up -d
          docker compose ps

      - name: Reconnect Prometheus to app networks
        run: |
          docker network connect qa-api_default cs3-prometheus || true
          docker network connect qa-local_default cs3-prometheus || true

      - name: Wait for services
        run: |
          sleep 20

      - name: Check qa-local health
        run: |
          docker run --rm --network host curlimages/curl:8.10.1 -fsS http://127.0.0.1:22034/health

      - name: Check qa-api health
        run: |
          docker run --rm --network host curlimages/curl:8.10.1 -fsS http://127.0.0.1:22014/health

      - name: Check qa-local metrics
        run: |
          docker run --rm --network host curlimages/curl:8.10.1 -fsS http://127.0.0.1:22104/metrics | grep qa_local

      - name: Check qa-api metrics
        run: |
          docker run --rm --network host curlimages/curl:8.10.1 -fsS http://127.0.0.1:22114/metrics | grep qa_api

      - name: Check node-exporter metrics
        run: |
          docker run --rm --network host curlimages/curl:8.10.1 -fsS http://127.0.0.1:22125/metrics | grep node_

      - name: Check Prometheus ready
        run: |
          docker run --rm --network host curlimages/curl:8.10.1 -fsS http://127.0.0.1:22124/-/ready

      - name: Check Prometheus targets
        run: |
          docker run --rm --network host curlimages/curl:8.10.1 -fsS http://127.0.0.1:22124/api/v1/targets > targets.json
          cat targets.json | head -200
          grep -q '"health":"up"' targets.json

      - name: Check Prometheus logs for fresh Grafana remote_write errors
        run: |
          docker logs cs3-prometheus --since 2m > prometheus.log 2>&1 || true
          cat prometheus.log
          if grep -Ei "401|403|unauthorized|forbidden|remote write.*failed|non-recoverable error|server returned HTTP status" prometheus.log; then
            echo "Fresh Grafana remote_write/auth error found"
            exit 1
          fi

      - name: Show running containers
        run: |
          docker ps
